language: c
sudo: false
addons:
  apt:
    packages:
    - bison
    - flex
    - dejagnu
compiler:
 - clang
 - gcc
before_install:
 - pip install --user cpp-coveralls
after_success:
 - coveralls --exclude t --exclude testsuite --gcov-options '\-lp'
script:
  - scons -k gcov=yes debug=yes filtergen test-binaries
  - runtest --all --debug -v -v
