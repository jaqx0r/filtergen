language: c
sudo: false
addons:
  apt:
    packages:
    - bison
    - flex
script: make -f makefile check
compiler:
 - clang
 - gcc
after_failure: cat t/test-suite.log
before_install:
 - pip install --user cpp-coveralls
after_success:
 - coveralls --exclude t --gcov-options '\-lp'
