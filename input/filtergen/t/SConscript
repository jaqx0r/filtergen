Import('env')

env.Distribute(env['DISTTREE'], ['SConscript',
								'scan.c',
								'parse.c',
								'emit.c',
								])

testcpppath = ['../../..', '..']
testlibpath = ['..']
testlibs = ['in_filtergen']
testcppflags= ['-Dyyparse=filtergen_parse',
			   '-Dyydebug=filtergen_debug',
			   ]

scan = env.UnitTest('scan.c',
					CPPPATH=testcpppath,
					LIBPATH=testlibpath,
					LIBS=testlibs,
					CPPFLAGS=testcppflags)

#test.Test('parse.c')
#test.Test('emit.c')
#env.Program('convert', source=['#t/convert.c', '#filter.o', '#fg-util.o'])
