Import('env')

sources = ['ir.c', ]

env.Distribute(env['DISTTREE'], sources + ['SConscript',
                                           'ir.h',
                                           'emit.c'])

t = env.Clone()

iremit = t.Program('iremit', source=sources + ['emit.c'])
t.Default(iremit)
